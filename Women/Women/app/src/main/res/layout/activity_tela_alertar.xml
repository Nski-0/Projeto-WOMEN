<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fundo"
    tools:context=".TelaAlertar">

    <Button
        android:id="@+id/ligar"
        android:layout_width="300dp"
        android:layout_height="120dp"
        android:layout_marginTop="72dp"
        android:background="@drawable/colorbutton"
        android:drawableLeft="@drawable/ic_phone_black_24dp"
        android:gravity="center_vertical"
        android:text="@string/ligar180"
        android:textColor="#ffffff"
        android:textSize="25sp"
        app:layout_constraintBottom_toTopOf="@+id/enviarMensagem"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.501"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.374" />

    <Button
        android:id="@+id/enviarMensagem"
        android:layout_width="300dp"
        android:layout_height="120dp"
        android:layout_marginBottom="132dp"
        android:background="@drawable/colorbutton"
        android:drawableLeft="@drawable/ic_email_black_24dp"
        android:gravity="center_vertical"
        android:text="@string/enviarMensagem"
        android:textColor="#ffffff"
        android:textSize="25sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/ligar"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/ligar" />

</androidx.constraintlayout.widget.ConstraintLayout>